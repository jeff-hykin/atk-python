(using_atk_version): 1.1.0

(package):
    (actions):
        (run): ruby install.rb
        (install): ruby install.rb

(project):
    name: Python installer
    description: The python installer for ATK
    
    (commands):
        (setup): echo 'no setup needed' 
    
    (paths):
        project_root: ./